import React from 'react'

const Login = (props) => {

  const { error, email, password, emailValidation, passwordValidate, handleSubmit, onKeyPress } = props;

  return (
    <div>
      <div>Login</div>
    </div>
  );
}

export default Login

import React from 'react'

const Signup = (props) => {

  const { error, email, password, confirmPassword, handleOnChange, handleSubmit, onKeyPress } = props;

  return (
    <div>Signup</div>
  )
}

export default Signup
